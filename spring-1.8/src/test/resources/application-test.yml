spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
  shell:
    interactive:
      enabled: false

  sql:
    init:
      mode: always
      continue-on-error: on
      data-locations: data.sql